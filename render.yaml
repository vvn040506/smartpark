services:
  - type: web
    name: smartpark
    runtime: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/smartpark-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
